[project]
name = "teo"
version = "0.1.0"
description = "The Teo programming languace"
requires-python = ">=3.14"

[project.scripts]
teo = "src.main:main"

[tool.setuptools.packages.find]
where = ["."]